package com.github.nez.ml;

//import org.tensorflow.*; find maven repo

public class MachineLearning {
//Deep neural networks, find trained models

        void hasAnomalyOccurred(){
            boolean war = false;
            //search for instance of the word war
//             -- sources, time, who is in the war,
//            filter?? weight insignificant countries and nations
            boolean naturalDisaster = false;
            boolean plague = false;
            boolean disruptionInGlobalSupplyChain = false;

            if(war || naturalDisaster || plague || disruptionInGlobalSupplyChain ){

            }
    // war, plague, natural disaster
    }
}
